<!-- Copyright (C) 2024 Claudia Wagner, Daniel Kuster -->
<UserControl x:Class="Loginator.Views.LogListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:Loginator.Controls"
             DataContext="{Binding LoginatorViewModel, Source={StaticResource Locator}}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <ListView SelectedItem="{Binding SelectedLog, Converter={StaticResource ViewModelToLogConverter}}"
              ItemsSource="{Binding Logs}"
              Name="lvLogs"
              SelectionMode="Single"
              controls:GridViewColumnResize.Enabled="True">
        <ListView.View>
            <GridView AllowsColumnReorder="False">
                <GridViewColumn Header="Timestamp" controls:GridViewColumnResize.Width="140">
                    <GridViewColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Timestamp, StringFormat='{}{0:dd.MM.yyyy HH:mm:ss.fff}', Converter={StaticResource TimestampConverter}}"
                                       Foreground="{Binding Level, Converter={StaticResource LevelToForegroundConverter}}" />
                        </DataTemplate>
                    </GridViewColumn.CellTemplate>
                </GridViewColumn>
                <GridViewColumn Header="Level" controls:GridViewColumnResize.Width="50">
                    <GridViewColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Path=Level.Name}"
                                       Foreground="{Binding Level, Converter={StaticResource LevelToForegroundConverter}}" />
                        </DataTemplate>
                    </GridViewColumn.CellTemplate>
                </GridViewColumn>
                <GridViewColumn Header="Message" controls:GridViewColumnResize.Width="*">
                    <GridViewColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Message, Converter={StaticResource SingleLineTextConverter}}"
                                       Foreground="{Binding Level, Converter={StaticResource LevelToForegroundConverter}}" />
                        </DataTemplate>
                    </GridViewColumn.CellTemplate>
                </GridViewColumn>
                <GridViewColumn Header="Context" controls:GridViewColumnResize.Width="100">
                    <GridViewColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Context, Converter={StaticResource SingleLineTextConverter}}"
                                       Foreground="{Binding Level, Converter={StaticResource LevelToForegroundConverter}}" />
                        </DataTemplate>
                    </GridViewColumn.CellTemplate>
                </GridViewColumn>
                <GridViewColumn Header="Process" controls:GridViewColumnResize.Width="50">
                    <GridViewColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Process}"
                                       Foreground="{Binding Level, Converter={StaticResource LevelToForegroundConverter}}" />
                        </DataTemplate>
                    </GridViewColumn.CellTemplate>
                </GridViewColumn>
            </GridView>
        </ListView.View>
        <!--<ListView.Resources>
            <Style TargetType="ListViewItem">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsVisible}" Value="False">
                        <Setter Property="ListBoxItem.Visibility" Value="Collapsed" />
                        <Setter Property="Height" Value="0" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </ListView.Resources>-->
    </ListView>
</UserControl>
