<!-- Copyright (C) 2024 Claudia Wagner, Daniel Kuster -->
<UserControl x:Class="Loginator.Views.LogListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:Loginator.Controls"
             xmlns:viewmodels="clr-namespace:Loginator.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:LoginatorViewModel}"
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding LoginatorViewModel, Source={StaticResource Locator}}">

    <ListView SelectedItem="{Binding SelectedLog, Converter={StaticResource ViewModelToLogConverter}}"
              ItemsSource="{Binding Logs}"
              Name="lvLogs"
              SelectionMode="Single"
              controls:GridViewColumnResize.Enabled="True">
        <ListView.View>
            <GridView AllowsColumnReorder="False">
                <GridViewColumn Header="{DynamicResource lab.Timestamp}" controls:GridViewColumnResize.Width="140">
                    <GridViewColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Timestamp, StringFormat='{}{0:dd.MM.yyyy HH:mm:ss.fff}', Converter={StaticResource TimestampConverter}}"
                                       Foreground="{Binding Level, Converter={StaticResource LevelToForegroundConverter}}" />
                        </DataTemplate>
                    </GridViewColumn.CellTemplate>
                </GridViewColumn>
                <GridViewColumn Header="{DynamicResource lab.Level}" controls:GridViewColumnResize.Width="50">
                    <GridViewColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Level.Name}"
                                       Foreground="{Binding Level, Converter={StaticResource LevelToForegroundConverter}}" />
                        </DataTemplate>
                    </GridViewColumn.CellTemplate>
                </GridViewColumn>
                <GridViewColumn Header="{DynamicResource lab.Message}" controls:GridViewColumnResize.Width="*">
                    <GridViewColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Message, Converter={StaticResource SingleLineTextConverter}}"
                                       Foreground="{Binding Level, Converter={StaticResource LevelToForegroundConverter}}" />
                        </DataTemplate>
                    </GridViewColumn.CellTemplate>
                </GridViewColumn>
                <GridViewColumn Header="{DynamicResource lab.Context}" controls:GridViewColumnResize.Width="100">
                    <GridViewColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Context, Converter={StaticResource SingleLineTextConverter}}"
                                       Foreground="{Binding Level, Converter={StaticResource LevelToForegroundConverter}}" />
                        </DataTemplate>
                    </GridViewColumn.CellTemplate>
                </GridViewColumn>
                <GridViewColumn Header="{DynamicResource lab.Process}" controls:GridViewColumnResize.Width="50">
                    <GridViewColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Process}"
                                       Foreground="{Binding Level, Converter={StaticResource LevelToForegroundConverter}}" />
                        </DataTemplate>
                    </GridViewColumn.CellTemplate>
                </GridViewColumn>
            </GridView>
        </ListView.View>
        <!--<ListView.Resources>
            <Style TargetType="ListViewItem">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsVisible}" Value="False">
                        <Setter Property="ListBoxItem.Visibility" Value="Collapsed" />
                        <Setter Property="Height" Value="0" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </ListView.Resources>-->
    </ListView>
</UserControl>
